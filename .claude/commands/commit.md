---
description: Quickly commit current changes
allowed-tools: Bash(git *)
---

1. Run `git status` to see changes
2. Run `git diff --stat` to understand the scope of changes
3. Generate a concise commit message (in English) based on the changes
4. Execute `git add -A && git commit -m "Your generated message"`
